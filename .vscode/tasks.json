{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "C++OpenGL",
            "type": "shell",
            "command": "g++",
            "args": [
                "${relativeFile}", "-g", "-lglut", "-lGLU", "-lGL"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}